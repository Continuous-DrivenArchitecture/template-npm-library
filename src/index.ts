/**
 * Placeholder public API.
 *
 * This function carries no domain logic — it exists only to give the
 * template a real, working export: something that compiles, builds,
 * ships type declarations, gets packed, and can be imported by a
 * consumer that only has the published package in node_modules.
 *
 * Replace this with the library's actual public API. Whatever you
 * export from this file (or add as further modules re-exported here)
 * becomes the package's public surface via the `exports` map in
 * package.json — see CONTRIBUTING.md.
 */
export function add(a: number, b: number): number {
  return a + b;
}
